# ── Google OAuth (public — safe to expose in browser bundle) ─────────────────
# Must match the Client ID in your Google Cloud Console OAuth 2.0 credential.
# Authorised redirect URI to register there: http://localhost:3000/auth/callback
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
